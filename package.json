{
  "name": "imrobot-frontend",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@imrobot/eslint-config": "workspace:^0.0.0",
    "eslint": "^8.36.0",
    "prettier": "^2.8.6",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@imrobot/components": "workspace:*"
  },
  "packageManager": "pnpm@7.28.0"
}
